# Human Ownership Sprints

1. Extend the validator to verify evidence claim IDs against source IDs and write regression tests.
2. Diagnose a selected target whose pack is missing or whose draft lacks the send block.
3. Explain the dependency graph and why artifact production cannot precede a frozen source boundary.
4. Add a live-link verification adapter that respects timeouts, robots, and no-login constraints.
5. Run a cold-operator test from workspace initialization to a fully validated approval queue.

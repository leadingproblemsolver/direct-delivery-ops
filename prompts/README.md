# Source prompt provenance

These two Markdown files are preserved operator-supplied execution contracts. The Python package adds deterministic workspace initialization, validation, release-readiness checks, manifests, and human-approval enforcement around them.
